node_modules/
dist/

# Runtime state written by the running APIs (per-deployment data, not source)
status-hub/announcements.json
status-hub/text_requests.json

# Local-only env files (real values live in ~/DockerApps/.env)
.env
.env.local
*.env
!.env.example

# Editor / OS noise
.DS_Store
.vscode/
.idea/
*.swp
